"""Stdlib-only MCP server exposing reasoning-graph to Claude Code (house pattern:
declared_core/examples/mcp_server.py, ~70 lines, zero deps).

OPUS-FILLS (Phase 7): tools = resolve, loop_scan, frontier_rate — each a thin
subprocess call to the reasoning-graph CLI with --json. See docs/10.
"""
raise NotImplementedError("OPUS-FILLS: Phase 7")
