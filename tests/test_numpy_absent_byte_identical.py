"""OPUS-FILLS (Phase 3/7 (subprocess with numpy hidden vs present)). Named tests are the contract — see tests/INVARIANTS.md."""
import pytest

pytestmark = pytest.mark.skip(reason="OPUS-FILLS: Phase 3/7 (subprocess with numpy hidden vs present) — implement against the module docstring contract")

def test_inv_byte_identical(tiny_instance): ...
