# RELEASE-NOTES

## 0.1.0 — PoC

<!-- OPUS-FILLS: what shipped, gate results (which of G0–G8 green), A/B headline numbers with claim scope. -->

### What's honest about the scope

<!-- OPUS-FILLS — required section, house pattern. Seed items that MUST appear if still true at release: path confidence is a score, not a calibrated probability; the A/B is N=30 PoC evidence on this corpus, not a generalizable benchmark (scale via the variant generator is ROADMAP); retirement is fixture-proven, not yet exercised by organic instance-0 usage; the 0.90 inherited-curation confidence default is a declared default awaiting Eyal's confirmation; frontier-rate baseline is 2 batches / 48 queries — small. -->
