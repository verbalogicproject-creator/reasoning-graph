"""OPUS-FILLS (Phase 3). Named tests are the contract — see tests/INVARIANTS.md."""
import pytest

pytestmark = pytest.mark.skip(reason="OPUS-FILLS: Phase 3 — implement against the module docstring contract")

def test_subprocess_adapter_provenance(tiny_instance): ...
def test_generic_adapter_smaller_surface_honest(tiny_instance): ...
