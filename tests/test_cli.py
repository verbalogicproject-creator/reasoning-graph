"""OPUS-FILLS (Phase 7). Named tests are the contract — see tests/INVARIANTS.md."""
import pytest

pytestmark = pytest.mark.skip(reason="OPUS-FILLS: Phase 7 — implement against the module docstring contract")

def test_every_subcommand_supports_json(): ...
def test_exit_codes_0_1_3_contract(): ...
